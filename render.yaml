services:
  - type: web
    name: dolphin-planner
    runtime: node
    buildCommand: npm run build
    startCommand: node --experimental-sqlite server/src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: ANTHROPIC_API_KEY
        sync: false
